const register = require('./register');
const login = require('./login');
const postPredictHandler = require('./postPredictHandler');

module.exports = {
    register,
    login,
    postPredictHandler,
};